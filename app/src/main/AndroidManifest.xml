<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Optional context signals — all opt-in, off by default. -->
    <!-- Standard runtime permission requested when the user enables location context. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
        PACKAGE_USAGE_STATS is a special AppOps permission (not a normal runtime permission).
        It must be declared here but is NOT granted via a permission dialog; the user must enable
        it manually via Settings → Apps → Special app access → Usage access. The app directs
        the user there when they enable the foreground-app context toggle.
        tools:ignore="ProtectedPermissions" suppresses the lint warning that this permission is
        only granted to system apps — it is actually grantable to user apps via usage access.
    -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".ChampiApplication"
        android:label="Champi"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Launched via an explicit Intent.setClassName from :overlay (see
             ai.champi.core.overlay.SETTINGS_ACTIVITY_CLASS), not an intent-filter — apps
             targeting API 31+ can't resolve implicit intents to a non-exported activity, even
             within the same app. -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <service
            android:name=".ChampiService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="overlay_assistant" />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".NotificationReplyReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ai.champi.app.action.NOTIFICATION_REPLY" />
            </intent-filter>
        </receiver>

        <service
            android:name=".ChampiTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile"
            android:label="@string/champi_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>
</manifest>
