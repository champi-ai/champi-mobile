plugins {
    id("champi.android.application")
}

android {
    namespace = "ai.champi.app"
    defaultConfig {
        applicationId = "ai.champi.app"
        versionCode = 1
        versionName = "0.1.0"
    }
}
